# Suối Tiên Bot — dependencies
# Cài: pip install -r requirements.txt

# Web framework
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.4
httpx==0.28.1

# LLM (provider chọn qua env LLM_PROVIDER / GEMINI_API_KEY — xem core/llm_client.py)
anthropic==0.105.2
google-genai==2.7.0
openai==2.30.0   # Grok (xAI) dung OpenAI SDK qua base_url https://api.x.ai/v1

# Retrieval
faiss-cpu==1.13.2
sentence-transformers==5.3.0
rank-bm25==0.2.2
numpy==2.4.4

# Auto updater (crawl + schedule)
APScheduler==3.11.2
beautifulsoup4==4.14.3
requests==2.34.1

# Config
python-dotenv==1.2.2

# Runtime pin — pydantic_core moi can Sentinel (typing_extensions >= 4.13)
typing_extensions>=4.13
